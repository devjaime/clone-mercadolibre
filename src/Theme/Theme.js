import { createMuiTheme } from '@material-ui/core/styles';
const palette = {
    primary: {
        main:"#FAF158", //Color Mercado Libre
        constrastText:"#ffffff"

    }
}

const Theme = createMuiTheme({
    palette,
})

export default Theme;